<!-- From: /home/xingwangzhe/桌面/staluxmyblog/AGENTS.md -->

# Frontmatter 写作规范

本文档描述了 stalux 博客中 `stalux/posts/` 目录下 Markdown 文章的 frontmatter 编写规范。

## 格式概述

所有文章必须使用 YAML 格式的 frontmatter，以 `---` 开头和结尾。

```yaml
---
title: 文章标题
date: "YYYY-MM-DD HH:MM:SS"
abbrlink: 短链接标识
tags:
    - 标签1
    - 标签2
categories:
    - 分类1
updated: "YYYY-MM-DD HH:MM:SS"
draft: false
---
```

## 字段说明

### 必填字段

| 字段       | 类型   | 说明                                                                 | 示例                                                 |
| ---------- | ------ | -------------------------------------------------------------------- | ---------------------------------------------------- |
| `title`    | string | 文章标题，与文件名一致（文件名去除扩展名）                           | `title: 在Linux上玩Flash网页游戏-洛克王国`           |
| `date`     | string | 发布日期，必须用双引号包裹，格式为 `YYYY-MM-DD HH:MM:SS`             | `date: "2026-03-27 14:30:00"`                        |
| `abbrlink` | string | 短链接标识，用于文章的永久链接。可以是语义化缩写，也可以是随机字符串 | `abbrlink: play-flash-luoke` 或 `abbrlink: cbab25fa` |

### 可选字段

| 字段         | 类型    | 说明                                                      | 默认值            |
| ------------ | ------- | --------------------------------------------------------- | ----------------- |
| `updated`    | string  | 最后更新日期，格式同 `date`                               | -                 |
| `tags`       | array   | 文章标签数组。**所有标签必须小写**。空数组使用 `tags: []` | `[]`              |
| `categories` | string  | 文章分类，**默认只允许一个，必须小写**                    | -                 |
| `draft`      | boolean | 是否为草稿，`true` 表示不发布                             | `false`           |
| `cc`         | string  | 版权协议                                                  | `CC-BY-NC-SA-4.0` |

### 自动生成字段

以下字段由 remark 插件在构建时自动生成，**不需要手动填写**：

- `desc` - 文章摘要
- `minutesRead` - 预计阅读时间
- `wordCount` - 文章字数

## 格式示例

### 完整示例

```yaml
---
title: 2025年终总结
abbrlink: cbab25fa
date: "2025-12-31 18:37:55"
updated: "2025-12-31 19:37:07"
categories: 年终总结
tags:
    - 记录
    - 年终总结
---
```

### 简洁示例（内联数组）

```yaml
---
title: Astro 5.17构建性能优化实践：从18s到13s
abbrlink: astro-517-performance-optimization
date: "2026-02-02 19:52:00"
tags: [Astro, 性能优化, 前端, ssg]
categories: 技术
---
```

### 新文章草稿示例

```yaml
---
title: 在Linux上玩Flash网页游戏-洛克王国
abbrlink:
date: ""
categories:
tags: []
---
```

## 缩进规范

- 使用 **4个空格** 缩进（不要使用 tabs）
- 数组块格式中，`-` 后面跟一个空格再写内容
- 字符串不需要引号，除非包含特殊字符或者日期格式（日期必须用引号）

## 验证

请参考 `content.config.ts` 中的 Zod schema 进行类型校验：

- `title` 必须是字符串
- `abbrlink` 必须是字符串或可转为字符串
- `date` 必须可转为 Date 类型
- `tags` 必须是字符串数组
- `draft` 默认为 `false`

---

# 写作风格指南

本指南总结了 stalux 博客的写作风格，确保 AI 生成的内容符合博主的个人风格。

## 一、整体风格定位

### 1.1 身份定位

- **身份**：计算机专业学生、开源爱好者、技术博主
- **立场**：自由软件支持者、信息安全关注者、终身学习者
- **态度**：务实、批判性思考、乐于分享、偶尔自嘲

### 1.2 语气特征

- **亲切自然**：像和朋友聊天，不使用过于正式的学术腔
- **热情直率**：对技术有热情，观点明确不模棱两可
- **幽默诙谐**：善用网络用语、段子,也不过分
- **真诚反思**：敢于承认自己的不足和错误

## 二、语言风格

### 2.1 用词特点

#### 强调方式

- 使用 `**加粗**` 强调关键词和重要概念
- 使用 `\``（代码格式）包裹表示术语或英文技术词汇
- 适当使用 `【】` 做补充说明或标注
- **斜体仅用于引用名言**，正文中不要使用斜体

示例：

```markdown
相比去年肯定是**更活跃**了，而且更熟悉现代化的 `git` 工程管理与团队协作。
这个浏览器非常简陋，界面也不美观，但能正常运行 `Docker` 就足够了。

> _"这是唯一使用斜体的地方——引用名言。_"
```

#### 中英混用规则

- **技术术语保留英文**：GitHub Actions、PR、RSS、API、LSP
- **工具名称保留英文**：Vue、React、Astro、Docker
- **文件名和路径**：使用英文，必要时加中文解释
- **避免不必要的英文**：普通描述性内容使用中文

#### emoji 使用

- 只能使用人脸黄脸Emoji
- 标题中可用 emoji 增加趣味性
- 列表项前可用 emoji 作为视觉标记
- 段落结尾可用 emoji 表达情绪

### 2.2 句式特点

#### 长短句结合

- 技术说明用短句，清晰直接
- 情感表达可用长句，有节奏感
- 避免连续使用相同长度的句子

#### 口语化表达

- 使用"说实话"、"说实话"、"不得不说"引出观点
- 使用"咋"、"啥"等口语词汇（适度）

### 2.3 段落结构

#### 技术文章结构

```
1. 前言/背景（为什么写这篇文章）
2. 问题分析（遇到了什么问题）
3. 解决方案（详细步骤）
4. 效果对比（前后对比表格）
5. 注意事项/总结
```

#### 随笔文章结构

```
1. 引入（个人经历或观察）
2. 展开（多角度分析）
3. 思考（深度观点）
4. 结语（总结或展望）
```

#### 学习笔记结构

```
1. 概念定义（简洁明了）
2. 公式/算法（使用 LaTeX）
3. 示例说明
4. 注意事项
```

## 三、内容风格

### 3.1 技术类文章

#### 代码展示

- 使用代码块并标注语言
- 添加文件名作为 title
- 关键行添加注释说明
- 对比展示"优化前"和"优化后"

示例：

```markdown title="content.config.ts (优化前)"
import { defineCollection } from "astro:content";
import { glob } from "astro/loaders";

const posts = defineCollection({
loader: glob({
pattern: ["*.{md,mdx}"],
base: "stalux/posts/",
// 注意：这里没有 retainBody 选项，默认为 true
}),
});
```

#### 数据呈现

- **优先使用表格**：凡是有多个项目对比、分类列举的内容，一律用表格呈现
- 不要分条设点（避免 `1. 2. 3.` 或 `- - -` 的列表形式）
- 使用引用块标注重要信息

示例：

```markdown
### 性能瓶颈

这个方案存在三个严重问题：

| 问题         | 影响                                                                         |
| ------------ | ---------------------------------------------------------------------------- |
| **重复存储** | `post.body` 保存了完整的原始 Markdown 文本，这部分数据在最终 HTML 中并不需要 |
| **重复计算** | 每次构建时，150+ 篇文章的字数、阅读时间都要重新计算                          |
| **数据传输** | 大量的 `body` 内容被序列化到构建数据中，增加了 I/O 开销                      |
```

**表格使用场景**：

- 攻防对比、前后对比、优劣对比
- 多个建议/措施的列举
- 分类展示不同维度的信息
- 时间线、数据指标展示

#### 引用规范

- 技术文档引用使用超链接
- 代码片段注明来源
- 他人观点明确标注出处

### 3.2 随笔类文章

#### 引入方式

- 从个人经历或具体事件引入
- 使用疑问句引发思考
- 引用名言或数据开篇

示例：

```markdown
> "robots need your body" —— 这不是科幻小说的开头，而是 rentahuman.ai 的首页标语。

## 一个荒诞的网站

当我在x上浏览,发现了这个网站,第一次打开 rentahuman.ai 时，我以为这是个玩笑。但它不是。
```

#### 观点表达

- 明确表达个人立场
- 用事实和数据支撑观点
- 承认对立观点的存在（但坚持己见）
- 避免绝对化表述（使用"可能"、"也许"、"某种程度上"）

#### 幽默元素

- 使用自嘲（"我的野路子自学"、"菜鸡"）
- 使用比喻（"代码可以放进博物馆玻璃柜"）
- 使用网络梗（"锟斤拷"、"坟头草三米高"）

### 3.3 学习笔记类

#### 概念解释

- 先给出定义，再展开说明
- 使用 LaTeX 公式表示数学概念
- 使用编号列表表示步骤

示例：

```markdown
## FIRST集

FIRST(X): 可以从X推导出的所有**串首终结符**构成的集合

如果 $X \Rightarrow {}^* \varepsilon$，那么 $\varepsilon \in \text{FIRST}(X)$

$$
\begin{align*}
\textcircled{1} &\ E \to TE' & \text{FIRST}(E) &= \lbrace (, \text{id} \rbrace \\
\textcircled{2} &\ E' \to +TE' \mid \varepsilon & \text{FIRST}(E') &= \lbrace +, \varepsilon \rbrace
\end{align*}
$$
```

#### 结构清晰

- 使用多级标题分层
- 使用 `<!--more-->` 截断摘要
- 使用列表和表格整理信息

## 四、格式规范

### 4.1 标题规范

#### 标题层级

- 文章主标题使用 h2 (`##`)
- 小节使用 h3 (`###`)
- 小节内的细分使用 h4 (`####`)
- 避免使用 h1 (`#`)，留给页面自动生成的标题

#### 标题风格

- 技术文章：简洁明了，突出核心内容
- 随笔文章：生动有趣，引发好奇心
- 学习笔记：准确描述知识点

示例：

```markdown
## 前言

## 问题分析：为什么构建这么慢？

### 原始方案的问题

#### 1. 重复存储
```

### 4.2 图片规范

#### 图片引用

- 使用相对路径 `/path/to/image.webp`
- 图片格式优先使用 webp
- 添加 alt 文字说明

#### 图片标注

- 图片下方使用引用块添加说明
- 截图类图片注明关键信息

示例：

```markdown
![洛克回忆](/qq/洛克王国回忆.webp)

> 到最后，我的**皇家圣光迪莫**还是没有升级到100级
```

### 4.3 链接规范

#### 外部链接

- 使用超链接格式 `[描述文字](URL)`
- 技术文档链接到官方文档
- 工具链接到 GitHub 或官网

#### 内部链接

- 使用相对路径 `/posts/abbrlink`
- 文章之间相互引用

#### 引用块

- 引用他人观点
- 添加重要说明
- 展示代码输出结果

#### 分割线

- 用于分隔不同主题段落
- 使用 `---` 创建

## 五、常用表达模板

### 5.1 开篇模板

技术教程：

```markdown
## 前言

在维护[项目名]的过程中，我发现[问题]。本文将详细介绍如何通过[方案]来解决这个问题。
```

随笔思考：

```markdown
> [名言或引发思考的句子]

## [主题]

当我[经历]时，我突然意识到[观点]。这让我开始思考[问题]。
```

学习笔记：

```markdown
本文总结了[课程/书籍]中关于[主题]的核心知识点。

## [概念名]

[简洁定义]
```

### 5.2 结尾模板

技术教程：

```markdown
## 总结

通过[方案]，我们实现了[效果]。这个方案特别适合[场景]。
如果你的项目也遇到类似问题，建议尝试这个方案。
```

随笔思考：

```markdown
## 结语

[总结观点]。

> [引用名言]
> —— [作者]
```

### 5.3 过渡句式

引出解决方案：

- "那么，如何解决这个问题呢？"
- "经过一番研究，我找到了一个可行的方案。"
- "下面是具体的解决步骤。"

转折表达：

- "然而，事情并没有那么简单。"
- "但问题恰恰出在这里。"
- "说到这里，不得不提另一个问题。"

总结归纳：

- "总的来说，..."
- "归根结底，..."
- "一言以蔽之，..."

## 六、避免的问题

### 6.1 语言层面

- ❌ 过度使用网络流行语（适度即可）
- ❌ 中英文混杂不规范（技术术语除外）
- ❌ 长句过多（一个句子控制在50字以内）
- ❌ 口语化过度（保持一定专业性）

### 6.2 内容层面

- ❌ 无根据的断言（要有事实或数据支撑）
- ❌ 过度负面（批判要有建设性）
- ❌ 抄袭或不注明引用
- ❌ 泄露敏感信息

### 6.3 格式层面

- ❌ 标题层级混乱
- ❌ 代码块无语言标注
- ❌ 图片无 alt 文字
- ❌ 链接失效

---

## 附录：常用分类和标签

### 分类（categories）

- 技术
- 胡思乱想
- 学校学习
- 年终总结
- GNU
- 游戏
- 记录

### 常用标签（tags）

- 学习、记录、笔记
- Astro、Vue、前端
- Linux、Ubuntu
- AI、思考
- 开源、自由软件
- 安全、隐私
- 游戏、娱乐
