---
import style from "@styles/components/posts/postCC.module.css";
import ccIcon from "@assets/cc-icon.svg?raw";
import { getCollection } from "astro:content";
import { createTranslator } from "@utils/i18n";
import { buildCCName, buildCCLink } from "@utils/cc";

interface Props {
    title: string;
    author: string;
    url: string;
    cc?: string;
}

const stalux = (await getCollection("config"))[0]?.data ?? {};
const { t } = createTranslator(stalux.lang || "zh-CN");
const { title, author, url, cc = "CC-BY-NC-SA-4.0" } = Astro.props;

const licenseLink = buildCCLink(cc);
const licenseName = buildCCName(cc, t);
---

<div class={style.ccBox}>
    <div class={style.ccContent}>
        <h2 class={style.ccTitle}>{title}</h2>
        <p class={style.ccAuthor}>{t("post.author")}{author}</p>
        {
            url && (
                <p class={style.ccUrl}>
                    {t("post.articleLink")}
                    <a href={url} target="_blank" rel="noopener noreferrer">
                        {url}
                    </a>
                </p>
            )
        }
		<p class={style.ccLicense}>
		    {t("post.licensedUnder")} <a
		        href={licenseLink}
		        target="_blank"
		        rel="noopener noreferrer"
		    >
		        {licenseName}
		    </a>{t("post.licenseSuffix")}
        </p>
    </div>

    <div class={style.ccWatermark}>
        <div class={style.ccIcon} set:html={ccIcon} />
    </div>
</div>
