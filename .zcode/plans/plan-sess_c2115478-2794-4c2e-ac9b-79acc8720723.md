在 `config.yml` 的 `stalux.links.sites` 数组末尾（最后一个条目 "UpXuu's blog" 之后）添加 Ytrox 的友链：

```yaml
- name: "Ytrox"
  description: "随缘分享点小东西"
  link: "https://ysone.top"
  icon: "https://ysone.top/assets/images/Ytrox.png"
```

保持与现有条目一致的缩进格式（12空格 + `- name`，字段缩进14空格）。
